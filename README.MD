Go to the frontend folder in the root:

run:

npm install (to install all dependencies)
npm run dev (to start the frontend)


then go to backend folder:

run:

npm install (to install all dependencies)
npm run dev (to start the backend)